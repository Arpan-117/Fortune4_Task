1> The backend is operational and can be used for CRUD operations with Postman
    1.1> The db.js file is used to establish database connection and the connection URL has to be given to interact with MongoDB. YOur own clusters can be used. Other details given in comments inside the db.js file

2> The front-end is for visual purpose only
    2.1> As there is no database connection and my database is empty, the product id cannot be retrieved from the front-end. This id is needed for making calls to the api for CRUD operartions.

3> Node modules folder and dot env files are not included for security reasons and larger app size.

4> Install the required dependencies by running npm install command from the root where package.json files are located.

5> Configure or read the package.json files to run the application as intended.